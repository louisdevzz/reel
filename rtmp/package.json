{
  "name": "rtmp",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "chmod +x install-ffmpeg.sh && ./install-ffmpeg.sh && chmod +x start-server.sh && ./start-server.sh",
    "dev": "bun --watch index.ts",
    "install-ffmpeg": "chmod +x install-ffmpeg.sh && ./install-ffmpeg.sh"
  },
  "dependencies": {
    "node-media-server": "^4.0.18"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node-media-server": "^2.3.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
